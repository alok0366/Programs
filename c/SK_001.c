#include<iostream>
#include<vector>
using namespace std;
void solve(long long int x,long long int y[]){}


int main(){
    cin.tie(0);
    cout.tie(0);
    ios::sync_with_stdio(0);
    int t;
    long long int x,y[t];
    while(t--){
            cin>>d;
        for(int i=0;i<x;i++){
            cin>>y[i];
        }
        solve(x,y);
    }
    return 0;
    }
